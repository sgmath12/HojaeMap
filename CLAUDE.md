# CLAUDE.md

## 프로젝트

전국 부동산 호재 시각화 지도. 빌드·번들러·패키지 매니저가 없습니다. 새 의존성을 추가하지 말고 CDN + 정적 파일 방식을 유지하세요.

- `realestate-map_new.html` — **본체. 작업은 이 파일에 합니다.**
- `data-regions.js` — 지역 데이터. 여기만 고치면 지도 내용이 바뀝니다.
- `realestate-map.html` — 구버전. 동작하지 않으며 수정하지 않습니다.

## 실행

`file://`로 열면 `fetch()`가 CORS로 차단되어 지도가 안 뜹니다. 반드시:

```bash
python3 -m http.server 8123   # → http://localhost:8123/realestate-map_new.html
```

## 행정구역 코드

모든 지역 식별은 통계청 행정구역 코드로 합니다. **계층이 코드 길이에 그대로 담겨 있어** 문자열 자르기로 상위 지역을 구합니다. 로마자 이름으로 매칭하지 마세요 (표기 흔들림·오타가 잦습니다).

```
시도     2자리   "31"        경기도
시군구   5자리   "31023"     성남시분당구
행정동   7자리   "3102378"   정자동
```

`lookup(code)`는 해당 코드에 데이터가 없으면 상위 단계로 거슬러 올라가 상속하고, `own:false`와 상속 출처를 함께 돌려줍니다. UI는 이때 "○○ 기준 참고값"을 표시해야 합니다 — 상속값을 그 지역 고유값처럼 보여주면 안 됩니다.

## 구조

`realestate-map_new.html`의 스크립트 순서:

1. `scoreStyle()` — 점수 → 색상/등급
2. `CAT_LABELS` / `CAT_COLORS` / `CAT_DESC` — 9개 지표 메타
3. `lookup()` / `pathOf()` / `info()` — 코드 계층 조회
4. Leaflet 초기화, 툴팁, 사이드바 렌더
5. `buildLayer()` / `toGeo()` — 레이어 빌더, TopoJSON 변환
6. 3단계 레이어 전환 (`TH_SGG = 9`, `TH_DONG = 12`), 동 경계 지연 로딩
7. 필터 칩 핸들러
8. 시도·시군구 경계 로드

## 실측 지표

`data-metrics.js`는 **자동 생성 파일이라 직접 고치면 안 됩니다.** `tools/build-metrics.py`가
OpenStreetMap 좌표로 행정동 3,504개 전부를 계산합니다.

- `transport`만 손으로 매긴 점수를 대체합니다. `info()`에서 `view` 객체를 만들 때
  덮어씁니다 — 상속받은 지역이라도 교통은 그 동의 자기 값을 씁니다.
- `school_access`·`life_density`는 **이름이 뜻하는 것과 재는 것이 다릅니다.**
  전자는 학원가가 아니라 학교 밀도(≈인구 밀도), 후자는 거주 쾌적성이 아니라 상업
  POI 밀도입니다. 학군·라이프 점수로 승격시키지 마세요. 참고 정보로만 노출합니다.
- 거리 계산의 기준점은 폴리곤 중심이 아니라 **활동 중심점**입니다. 동 내부 격자
  점들을 반경 400m 내 POI 수로 가중한 값입니다. 기하학적 중심을 쓰면 산·논밭까지
  평균이 되어 구미1동 같은 곳이 2,296m로 잘못 나옵니다.

`data-redev.js`도 자동 생성입니다 (`tools/build-redev.py`, 원본은 `data/*.csv`).

- **준공·청산은 집계에서 뺍니다.** 끝난 사업은 호재가 아닙니다.
- **규모는 구역 수가 아니라 공급 세대수로 잽니다.** 구역 수로 세면 종로구
  (70구역·3,678세대, 상업지 소규모)가 강남구(26구역·37,514세대, 아파트 재건축)를
  이깁니다. 주거 호재 지도에서는 틀린 답입니다.
- 법정동↔행정동이 어긋나 못 잇는 곳은 **억지로 잇지 않고 비워둡니다.** 상위
  시군구 값을 쓰되 "구 전체 기준"이라고 밝힙니다.
- redev는 `develop`·`policy` 점수를 **덮어쓰지 않습니다.** 정비사업은 개발의
  일부일 뿐이라(신도시·산업단지는 안 잡힘), 정비사업이 없다고 개발 호재가 없는
  건 아닙니다. 별도 실측 항목으로만 보여줍니다.

새 지표를 붙일 때는 **반드시 아는 지역으로 검증하고, 안 맞으면 이름을 바꾸거나
버리세요.** 그럴듯한 이름을 붙여 점수로 승격시키는 것이 가장 나쁜 결과입니다.
transport는 검증을 통과해 점수를 대체했고, school_access·life_density는 떨어져
참고용으로 남았고, redev는 세대수 기준으로 고친 뒤에야 쓸 만해졌습니다.

## 이야기 카드 (사이드바)

**이 제품의 본체는 지도가 아니라 지역을 눌렀을 때 뜨는 카드입니다.** 지도는 들어오게
만드는 미끼고, 카드가 읽히는 물건입니다. 대상 사용자는 부동산 초년생이라 숫자를
던지면 아무 의미가 없습니다.

- 점수는 **오른쪽 위 배지 하나로만** 남깁니다. 본문에 "82점" 같은 숫자를 쓰지 마세요.
- 본문은 `buildStory()`가 만드는 이야기입니다. 규칙은 하나 — **점수를 말하지 말고
  그 점수가 생활에서 뭘 뜻하는지 말한다.** 학군 96점(X) → "전국구 학원가가 걸어갈
  거리에 있어요"(O).
- 강점 최대 3개 + 약점 최대 2개. 첫 강점에 "👉 이게 여기서 제일 커요"가 붙습니다.
- 어투는 "친구가 설명해주듯" 반말 아닌 해요체. UI 문구 전체가 이 톤입니다.
- 상세 숫자(9개 지표 바, 실측 정보, 원본 뉴스)는 `<details>`로 접어둡니다. 지우지는
  마세요 — 검증할 때 필요합니다.
- 문구 템플릿은 `BEATS` 상수에 있습니다. 새 지표를 추가하면 여기에도 문구를 넣어야
  카드에 나옵니다.
- **약점에는 뉴스를 붙이지 않습니다.** 유형만 맞으면 붙는 구조라 "정비사업 기대 낮음"
  밑에 "구역 지정" 같은 호재가 달려 앞뒤가 안 맞았던 적이 있습니다.

## 종합점수는 계산식입니다

`baseScore()` = 9개 지표의 가중평균(`WEIGHTS`, 합 1.0). 여기에 시세를 28% 섞어
최종 점수가 나옵니다. **손으로 정한 숫자가 아닙니다.**

이렇게 바꾼 이유는 카드에서 "왜 이 점수인지"를 보여주기 위해서입니다.
`contributions()`가 지표별 기여분을 돌려주고, 카드의 `.why` 블록이 막대로
그립니다. 막대 길이를 다 더하면 종합점수가 됩니다.

`data-regions.js`의 `score` 필드는 이제 표시에 쓰이지 않습니다(참고용). 점수를
바꾸고 싶으면 개별 지표를 고치세요.

## UI에서 뺀 것들 — 다시 제안하지 마세요

사용자가 명시적으로 빼달라고 한 것들입니다.
- **안개** (안 가본 곳을 회색으로 덮기)
- **탐험 현황 패널** (진행률 바, 오늘의 동네) — 맨 위 공간이 아깝다고 했습니다
- **범례·필터 접기** — 접지 말고 항상 펼쳐 두세요

관심 지역(⭐)과 방문 기록(`localStorage`)은 남아 있습니다.

## 모바일

- ≤720px에서 사이드바가 바텀시트가 됩니다.
- **지도는 `inset:0`으로 화면 전체를 채우고 시트가 그 위에 얹힙니다.** 지도를
  시트 위쪽에만 두면 시트를 내렸을 때 빈 공간만 생깁니다(실제로 그랬습니다).
- 시트 높이가 바뀌면 반드시 `syncMap()`을 부르세요. `map.invalidateSize()`를
  안 하면 Leaflet이 예전 크기를 기억해 타일이 어긋납니다.

## 실거래가

`data-prices.js`는 자동 생성입니다 (`tools/fetch-prices.py`).

- 인증키는 `data/api_key.md` — **`.gitignore`에 있습니다. 커밋하지 마세요.**
- 해제 거래(`cdealType='O'`)는 뺍니다. 취소된 계약이라 시세가 아닙니다.
- 평균이 아니라 **중앙값**입니다.
- 코드표 검증에 `estateAgentSggNm`(중개사 소재지)을 쓰면 안 됩니다. 매물 위치와
  달라서 멀쩡한 코드가 틀린 것처럼 나옵니다. `sggCd`로 대조하세요.
- 실거래가는 **종합점수에 28% 반영**됩니다 (`PRICE_W`). 나머지 72%가 호재 점수입니다.
  가격을 아예 빼면 반도체 산단 하나로 농촌 지역이 마포·양천 위로 올라가고, 너무 많이
  넣으면 "지금 싼데 뜨는 곳"을 못 찾아줍니다. 그 사이를 잡은 값이니 함부로 바꾸지 마세요.
- **가격 점수는 로그 척도입니다** (`priceScore`). 백분위로 바꾸면 안 됩니다 — 전국
  214곳 중 대부분이 지방이라 대구 서구(평당 1,600만)가 77번째로 올라갑니다. 강남의
  1/7 가격인데도요.
- `index.html`은 `realestate-map_new.html`의 사본입니다 (GitHub Pages 진입점).
  본체를 고친 뒤 `cp realestate-map_new.html index.html` 로 맞춰주세요.

## 상속과 SIDO_BASE

데이터 없는 지역은 상위 단계를 상속하는데, **시도에서 물려받을 때는 시도 점수가
아니라 `SIDO_BASE`의 외곽 기준값을 씁니다.**

시도 점수는 그 지역 전체를 대표하는 값이라 상위 지역이 끌어올립니다. 경기 70은
판교·용인·과천이, 충남 58은 천안·아산이 만든 값입니다. 이걸 그대로 물려주면
**이천시가 서울 양천구와 동점 70**이 되는 일이 실제로 있었습니다.

**같은 문제가 시군구→동 층에도 있어 `SGG_DECAY`로 막습니다.** 구 점수는 그 구의
대표 지역이 만든 값이라, 데이터 없는 동이 그대로 받으면 부풀려집니다. 영등포구 80은
여의도가 만든 값인데 대림동까지 80을 받아 **문래동이 신정동보다 높게** 나왔습니다.
동으로 물려줄 때는 구별 비율만큼 낮춥니다(기본 0.90, 여의도를 낀 영등포는 0.86).

새 시군구 데이터를 채워 넣으면 그 시도의 남은 지역이 더 부풀려지므로, 대도시를
추가할 때는 `SIDO_BASE`의 해당 값도 같이 내려야 하는지 확인하세요. 마찬가지로
대표 지역과 나머지 격차가 큰 구를 채울 때는 `SGG_DECAY`를 확인하세요.

데이터를 늘릴 때는 이런 관계가 유지되는지 꼭 확인하세요:
- 시군구: 서울 양천구 > 서울 구로구 > 경기 이천시 > 충남 보령시 > 전남 완도군
- 동: 여의동 > 신정7동 > 문래동 > 대림1동

## 규칙

- **점수를 근거 없이 지어내지 마세요.** 현재 값도 추정치이며 README에 그렇게 명시되어
  있습니다. 데이터를 늘릴 때는 그 지역에 실제로 무엇이 있는지에 근거해야 하고,
  추정치라는 표시를 지우면 안 됩니다.
- **결론을 정해두고 근거를 찾지 마세요.** 초기 버전이 분당구에서 정자동을 1위로 둔 것이
  그 사례입니다. 사용자가 예로 든 동네였고, 왜 높은지를 설명할 근거(역세권)를 찾아
  거기 맞춰 나머지 지표를 채웠습니다. 정작 가장 큰 호재인 1기 신도시 선도지구 지정은
  정책 점수가 80~82로 평평해 전혀 반영되지 않았습니다. 한 지역을 채울 때는 이웃 지역과
  **어느 지표에서 갈리는지**를 먼저 확인하세요.
- **데이터를 추가할 땐 코드를 먼저 검증하세요.** GeoJSON에 없는 코드나 코드-이름 불일치는 조용히 렌더링에서 빠집니다. `data-regions.js`의 키를 `geo-*.json`의 `properties.code`와 대조하는 검증을 반드시 돌리세요.
- 한글 UI 프로젝트입니다. 주석·라벨·커밋 메시지는 한국어로 씁니다.

## 색상에 대한 맥락

현재 팔레트는 빨강(저점수)~파랑(고점수) 대립형이고 "위험"·"심각" 등급 라벨을 씁니다. 이게 지역 낙인처럼 읽힐 수 있다는 점은 논의됐고, **시인성을 우선해 현행 유지하기로 사용자가 결정했습니다.** 요청 없이 팔레트를 바꾸지 마세요.

검토했던 대안(기록용): 파랑 단색 농도로 전환 / 색의 의미를 점수가 아닌 호재 건수로 전환 / 행정구역을 칠하지 않고 호재 위치에 핀·히트맵.

## 경계 데이터 주의사항

- `geo-*.json`은 **TopoJSON**입니다. GeoJSON이 아니므로 `topojson.feature()`로 변환해야 합니다 (`toGeo()` 참고). 같은 데이터의 GeoJSON은 60MB인데 TopoJSON은 6.9MB입니다.
- `geo-dong.json`은 4MB라 **처음 동 줌에 도달할 때 지연 로딩**합니다. 초기 로드에 넣지 마세요.
- 파일이 크니 통째로 `Read`하지 말고 `python3 -c`로 필요한 속성만 뽑아 확인하세요.
- 구버전 `skorea-*-geo.json`(GADM)은 미사용입니다. 세종특별자치시가 누락되어 있고 로마자 이름 기반이라 교체했습니다.
