# CLAUDE.md

## 프로젝트

전국 부동산 호재 시각화 지도. **단일 HTML 파일**로 동작하고 빌드·번들러·패키지 매니저가 없습니다. 새 의존성을 추가하지 말고 CDN + 인라인 스크립트 방식을 유지하세요.

- `realestate-map_new.html` — 현행 버전. **작업은 항상 이 파일에 합니다.**
- `realestate-map.html` — 구버전 (4개 지표). 참고용이며 수정하지 않습니다.

## 실행

`file://`로 열면 `fetch()`가 CORS로 차단되어 지도가 안 뜹니다. 반드시:

```bash
python3 -m http.server 8123   # → http://localhost:8123/realestate-map_new.html
```

## 구조

HTML 하나에 CSS·JS·데이터가 전부 인라인입니다. 스크립트 구역 순서:

1. `scoreStyle()` — 점수 → 색상/등급 매핑
2. `SIDO` / `SIGUNGU` — 지역 데이터 상수
3. `CAT_LABELS` / `CAT_COLORS` / `CAT_DESC` — 9개 지표 메타
4. `getSidoReg()` / `getSigunguReg()` — GeoJSON feature → 데이터 매핑
5. Leaflet 초기화, 툴팁, 사이드바 렌더, 레이어 빌더
6. 줌 레벨(`ZOOM_TH = 9`) 기준 시도 ↔ 시군구 레이어 전환
7. 필터 칩 핸들러
8. `Promise.all`로 GeoJSON 2개 로드

## 규칙

- **데이터는 예시값입니다.** 호재 점수나 뉴스를 새로 지어내지 마세요. 데이터가 없는 지역은 회색 "데이터 준비중"으로 두는 것이 맞습니다. 시도 점수를 시군구에 상속시켜 채우면 경기도 전역이 한 색으로 보이는 문제가 생깁니다.
- **색상은 파랑 단일 색상의 농도**로만 표현합니다. 빨강↔파랑 대립 팔레트나 "위험"·"심각" 같은 등급 라벨은 지역 낙인처럼 읽히므로 쓰지 않습니다. 문구는 "호재 적음 / 보통 / 호재 많음"처럼 정보량 서술로 씁니다.
- 지역 매칭은 GeoJSON `properties`의 값에 정확히 의존합니다. 키를 바꿀 때는 실제 GeoJSON을 파싱해 값을 확인하고 바꾸세요. 로마자 표기는 오타·표기 흔들림이 잦습니다.
- 한글 UI 프로젝트입니다. 주석·라벨·커밋 메시지는 한국어로 씁니다.

## GeoJSON 주의사항

- 파일이 각각 15~18MB입니다. 통째로 `Read`하지 말고 `python3 -c` 등으로 필요한 속성만 뽑아 확인하세요.
- 구버전 GADM 데이터에는 **세종특별자치시가 없습니다**.
- 통계청 2018 데이터(`southkorea/southkorea-maps`)는 시도 17개(세종 포함), 시군구 250개이며 `properties`가 `{ name, name_eng, code, base_year }` 형식의 한글명 + 행정코드입니다. 성남시분당구·용인시수지구처럼 **구 단위까지 분리**되어 있습니다.
