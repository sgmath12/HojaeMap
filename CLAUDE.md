# CLAUDE.md

## 프로젝트

전국 부동산 호재 시각화 지도. 빌드·번들러·패키지 매니저가 없습니다. 새 의존성을 추가하지 말고 CDN + 정적 파일 방식을 유지하세요.

- `realestate-map_new.html` — **본체. 작업은 이 파일에 합니다.**
- `data-regions.js` — 지역 데이터. 여기만 고치면 지도 내용이 바뀝니다.
- `realestate-map.html` — 구버전. 동작하지 않으며 수정하지 않습니다.

## 실행

`file://`로 열면 `fetch()`가 CORS로 차단되어 지도가 안 뜹니다. 반드시:

```bash
python3 -m http.server 8123   # → http://localhost:8123/realestate-map_new.html
```

## 행정구역 코드

모든 지역 식별은 통계청 행정구역 코드로 합니다. **계층이 코드 길이에 그대로 담겨 있어** 문자열 자르기로 상위 지역을 구합니다. 로마자 이름으로 매칭하지 마세요 (표기 흔들림·오타가 잦습니다).

```
시도     2자리   "31"        경기도
시군구   5자리   "31023"     성남시분당구
행정동   7자리   "3102378"   정자동
```

`lookup(code)`는 해당 코드에 데이터가 없으면 상위 단계로 거슬러 올라가 상속하고, `own:false`와 상속 출처를 함께 돌려줍니다. UI는 이때 "○○ 기준 참고값"을 표시해야 합니다 — 상속값을 그 지역 고유값처럼 보여주면 안 됩니다.

## 구조

`realestate-map_new.html`의 스크립트 순서:

1. `scoreStyle()` — 점수 → 색상/등급
2. `CAT_LABELS` / `CAT_COLORS` / `CAT_DESC` — 9개 지표 메타
3. `lookup()` / `pathOf()` / `info()` — 코드 계층 조회
4. Leaflet 초기화, 툴팁, 사이드바 렌더
5. `buildLayer()` / `toGeo()` — 레이어 빌더, TopoJSON 변환
6. 3단계 레이어 전환 (`TH_SGG = 9`, `TH_DONG = 12`), 동 경계 지연 로딩
7. 필터 칩 핸들러
8. 시도·시군구 경계 로드

## 규칙

- **점수를 근거 없이 지어내지 마세요.** 현재 값도 개발계획·역세권·학원가 분포에 근거한 추정치이며 README에 그렇게 명시되어 있습니다. 데이터를 늘릴 때는 그 지역에 실제로 무엇이 있는지에 근거해야 하고, 추정치라는 표시를 지우면 안 됩니다.
- **데이터를 추가할 땐 코드를 먼저 검증하세요.** GeoJSON에 없는 코드나 코드-이름 불일치는 조용히 렌더링에서 빠집니다. `data-regions.js`의 키를 `geo-*.json`의 `properties.code`와 대조하는 검증을 반드시 돌리세요.
- 한글 UI 프로젝트입니다. 주석·라벨·커밋 메시지는 한국어로 씁니다.

## 색상에 대한 맥락

현재 팔레트는 빨강(저점수)~파랑(고점수) 대립형이고 "위험"·"심각" 등급 라벨을 씁니다. 이게 지역 낙인처럼 읽힐 수 있다는 점은 논의됐고, **시인성을 우선해 현행 유지하기로 사용자가 결정했습니다.** 요청 없이 팔레트를 바꾸지 마세요.

검토했던 대안(기록용): 파랑 단색 농도로 전환 / 색의 의미를 점수가 아닌 호재 건수로 전환 / 행정구역을 칠하지 않고 호재 위치에 핀·히트맵.

## 경계 데이터 주의사항

- `geo-*.json`은 **TopoJSON**입니다. GeoJSON이 아니므로 `topojson.feature()`로 변환해야 합니다 (`toGeo()` 참고). 같은 데이터의 GeoJSON은 60MB인데 TopoJSON은 6.9MB입니다.
- `geo-dong.json`은 4MB라 **처음 동 줌에 도달할 때 지연 로딩**합니다. 초기 로드에 넣지 마세요.
- 파일이 크니 통째로 `Read`하지 말고 `python3 -c`로 필요한 속성만 뽑아 확인하세요.
- 구버전 `skorea-*-geo.json`(GADM)은 미사용입니다. 세종특별자치시가 누락되어 있고 로마자 이름 기반이라 교체했습니다.
